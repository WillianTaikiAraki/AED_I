#include <stdio.h>

int main() {
    int A, B, SOMA;

    // Lê os dois valores inteiros
    scanf("%d", &A);
    scanf("%d", &B);

    // Calcula a soma
    SOMA = A + B;

    // Imprime o resultado no formato solicitado
    printf("SOMA = %d\n", SOMA);

    return 0;
}
